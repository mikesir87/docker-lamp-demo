# Groceries App

In order to run this application, you need to have the following

- PHP server with the mysqli extension installed
- MySQL database with defaults:
    - Database name: `app_db`
    - Database user: `root`
    - Database password: `password`

Load the schema found in `db/create-schema.sql`.

Open the browser to the site
